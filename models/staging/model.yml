version: 2


models:
  - name: stg_sales
    description: "A starter dbt model"
    columns:
      - name: sale_id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null

  - name: stg_products
    description: "A starter dbt model products"
    columns:
      - name: product_id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null